# Manager

Manager is an active portal, but its current runtime still uses the older `manager-v11.html` shell and Workforce patch chain.

Manager refactor is intentionally postponed until Owner Workforce is stabilized.

Do not copy Owner Workforce fixes into Manager by adding more root-level patch files. Manager will be migrated to the same Shared Core + one-engine-per-module architecture in a separate phase.