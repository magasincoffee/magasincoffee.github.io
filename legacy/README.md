# Legacy

This folder contains files that are retained only for historical reference or as a temporary quarantine during migration.

Nothing under `legacy/` should be loaded by a canonical production runtime.

When an active module is fully migrated and verified, its obsolete root-level versions should be moved here or deleted.